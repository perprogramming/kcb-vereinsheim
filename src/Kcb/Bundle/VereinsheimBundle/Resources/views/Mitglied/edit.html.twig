<h1>Mitglied bearbeiten</h1>

<form action="{{ path('kcb_vereinsheim_mitglied_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Speichern</button>
    </p>
</form>

<h2>Was willst Du tun?</h2>
<ul class="record_actions">
    <li>
        <a href="{{ path('kcb_vereinsheim_mitglied_index') }}">
            Zurück zur Liste
        </a>
    </li>
    <li>
        <form action="{{ path('kcb_vereinsheim_mitglied_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Löschen</button>
        </form>
    </li>
</ul>
